A common set of steps need to be implemented as a sequence of abstract methods, 
Which get called from a single “execute” method. 
Different concrete implementations are provided by concrete subclasses. 